// Given an array of integers arr and an integer k create a boolean function
// such that it returns true if there exists 2 elements in the array which when we add we get k.
func findPair(arr, k) {
}
